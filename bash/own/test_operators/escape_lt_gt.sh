str1="apple"
str2="banana"

# Рабочий пример с [ ] (нужно экранировать <):
if [ "$str1" \< "$str2" ]; then
  echo "$str1 меньше $str2 по алфавиту"
fi

# Рабочий пример с [[ ]] (экранирование не нужно):
if [[ "$str1" < "$str2" ]]; then
  echo "$str1 меньше $str2 по алфавиту"
fi