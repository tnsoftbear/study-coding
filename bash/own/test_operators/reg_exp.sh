str="hello123"

# В [ ] регулярные выражения не поддерживаются:
if [ "$str" =~ ^hello[0-9]+$ ]; then
  echo "Совпадение найдено"
fi

# Рабочий пример с [[ ]]:
if [[ "$str" =~ ^hello[0-9]+$ ]]; then
  echo "Совпадение найдено"
fi