name: git_push
description: Git add, commit, push command-line application.
version: 1.0.0

environment:
  sdk: ^3.0.1

dependencies:
  intl: ^0.18.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
